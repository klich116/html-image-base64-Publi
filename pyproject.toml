[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "html_image_base64"
version = "1.0.0"
description = "Convierte imagenes referenciadas en archivos HTML a Base64 embebido (data URI)"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
html-image-base64 = "html_image_base64.cli:main"
